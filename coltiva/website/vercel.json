{
  "framework": "nextjs",
  "regions": ["fra1"],
  "installCommand": "corepack enable && corepack prepare pnpm@10.26.1 --activate && pnpm install --no-frozen-lockfile",
  "buildCommand": "corepack enable && corepack prepare pnpm@10.26.1 --activate && pnpm build",
  "outputDirectory": ".next",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet -- . ../../pnpm-workspace.yaml ../../package.json"
}
