# AERIS Aeryion Backend — runtime dependencies
# Slim by design: only what app/ and workers/ actually import.
# Last cleaned 2026-05-04 (was 129 lines from a Jupyter pip freeze).
# Versions match the working venv on the dev machine + Railway runtime.

# Web framework
fastapi==0.135.3
uvicorn[standard]==0.44.0
pydantic==2.12.5
pydantic-settings==2.13.1

# Environment / config
python-dotenv==1.2.2

# Database
supabase==2.28.3

# HTTP client (used by Coltiva → Aeryion calls + general use)
httpx==0.28.1

# Earth Engine + Google Cloud (workers only)
earthengine-api==1.7.21
google-cloud-storage==3.10.1

# Dev / CI tooling — unpinned because they don't ship to production
flake8
black
mypy
pytest
