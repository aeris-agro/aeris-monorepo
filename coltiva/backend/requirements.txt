# Coltiva backend — runtime dependencies
# Slim by design. Only what app/ and workers/ actually import.
# Last cleaned 2026-05-09.

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Environment / config
python-dotenv>=1.0.0

# Database
supabase>=2.3.0

# HTTP client (used by SmsClient + general use)
httpx>=0.26.0

# JWT signing for OTP signup tokens + session tokens
pyjwt>=2.8.0
